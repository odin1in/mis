= form_for(@admin_supplier, :html => { :class => "form-horizontal" }) do |f|
  - if @admin_supplier.errors.any?
    .alert.alert-danger.alert-dismissable
      button.close type="button" data-dismiss="alert" aria-hidden="true" 
        | &times;
      h4= "#{pluralize(@admin_supplier.errors.count,"error")} prohibited this admin_supplier from being saved:"
      ul
        - @admin_supplier.errors.full_messages.each do |msg|
          li= msg
  .form-group
    = f.label :name, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :name, :class => "form-control"
  .form-group
    = f.label :address, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :address, :class => "form-control"
  .form-group
    = f.label :phone, :class => "col-sm-2 control-label"
    .col-sm-10
      = f.text_field :phone, :class => "form-control"
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit :class => "btn btn-primary"
